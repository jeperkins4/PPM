<h2>Edit Incident</h2>

<%= error_messages_for :incident %>

<% form_for(:incident, :url => incident_path(@incident), :html => { :method => :put }) do |f| %>
 <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Show', incident_path(@incident) %> |
<%= link_to 'Back', incidents_path %>