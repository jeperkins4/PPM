<h2>New Complaint</h2>

<%= error_messages_for :complaint %>

<% form_for(:complaint, :url => complaints_path) do |f| %>
 <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', complaints_path %>