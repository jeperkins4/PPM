<h2>New Accountability Log Category</h2>

<%= error_messages_for :context %>

<% form_for(:context, :url => contexts_path) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', contexts_path %>