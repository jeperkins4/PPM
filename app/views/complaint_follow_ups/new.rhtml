<h2>Add Complaint Follow Up</h2>

<%= error_messages_for :complaint_follow_up %>
<% form_for(:complaint_follow_up, :url => complaint_follow_ups_path) do |f| %>
 <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', complaint_path(@complaint) %>