<p>
  <b>Incident date:</b>
  <%=h @incident.incident_date %>
</p>

<p>
  <b>Description:</b>
  <%=h @incident.description %>
</p>

<p>
  <b>Incident type:</b>
  <%=h @incident.incident_type.incident_type %>
</p>

<p>
  <b>Facility:</b>
  <%=h @incident.facility.facility %>
</p>

<p>
  <b>Investigator:</b>
  <% @investigator = Investigator.find_by_id(@incident.investigator_id) -%>
  <%=h @investigator.firstname + ' ' + @investigator.lastname  rescue 'Pending' %>
</p>

<p>
  <b>Contract manager notified:</b>
  <%=h ( @incident.contract_manager_notified == 1) ? 'Yes' : 'No' %>
</p>

<p>
  <b>Bureau notified:</b>
  <%=h ( @incident.bureau_notified == 1) ? 'Yes' : 'No' %>
</p>

<p>
  <b>Warden notified:</b>
  <%=h ( @incident.warden_notified  == 1) ? 'Yes' : 'No' %>
</p>

<p>
  <b>Investigation completed:</b>
  <%=h ( @incident.facility_investigation_complete  == 1) ? 'Yes' : 'No' %>
</p>

<p>
  <b>Action type:</b>
  <%=h @incident.action_type.action rescue 'Pending' %>
</p>

<%= link_to 'Edit', edit_incident_path(@incident) %> |
<%= link_to 'Back', incidents_path %>

<br />
<br />


<fieldset>
<legend>Follow Ups</legend>	
<% @follow_ups.each do |follow_up| -%>
 
    <b><%=h follow_up.follow_up_date %></b><br />
    <p><%=h follow_up.follow_up %>&nbsp;<%= link_to 'View', follow_up_path(@incident, follow_up) %></p>

 <hr> 
<% end -%>
</fieldset>
<p>
    <%= link_to 'Add Follow Up', new_follow_up_path(@incident)  %>
</p>